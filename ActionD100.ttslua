#include ActionDie

ACTION_FUNCTION = function(obj,value,color, colorText)
  local myName
  local otherName

  if(IS_TENS_DIE) then
    myName = "TENS"
    otherName = "ONES"
  else
    myName = "ONES"
    otherName = "TENS"
  end

  otherStartTime = 
